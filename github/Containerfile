# Podman-first reproduction image for the brand-alignment package.
# Build: podman build -t brand-align:2026-09-14 -f github/Containerfile .
FROM python:3.12-slim

WORKDIR /opt/align
COPY . /opt/align

RUN python -m pip install --no-cache-dir --upgrade pip

# Default command runs the local POSIX SHM demo and prints the disclaimer.
CMD ["python", "github/bench_shm.py"]
