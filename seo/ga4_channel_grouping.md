# GA4 custom channel — AI Discovery

Apply only after apex hosts return HTTP 200 without a password gate.

Channel group name: `AI Discovery`

Include regex:

```
(?i)(chatgpt|chat\.openai|perplexity|claude\.ai|gemini\.google|copilot\.microsoft|you\.com|phind|andi\.search|deepseek)
```

Place above Organic Search. Do not submit gated apex URLs to Search Console.
